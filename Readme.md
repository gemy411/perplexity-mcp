# Online Search MCP Server

## API Key Selection

Set one of these environment variables:

- `OPEN_ROUTER_API_KEY` → uses OpenRouter
- `PERPLEXITY_API_KEY` → uses Perplexity (PPLX)

If both are set, OpenRouter is used. If neither is set, the server will not run.
